# Open Ball Verifier

Interactive, causal, human-correctable analysis of a soccer-ball trajectory from broadcast video.

## Development

```sh
npm install
npm run dev
```

The browser computes local image-plane forecasts using only accepted observations before the displayed frame. This is not a calibrated 3-D physical reconstruction.
